# SEO / UI・UX / 正確性の改善提案（2026-08-10）— tools 側の作業分

サイト全体の調査結果と提案の全文は
**[hasokon-home/docs/proposals/2026-08-10-seo-uiux.md](https://github.com/ke-iwata/hasokon-home/blob/main/docs/proposals/2026-08-10-seo-uiux.md)**
にあります。このファイルは、そのうち **hasokon-tools で手を動かす分**です。

**この提案は実施していません。** 採用するものを選んでから、別PRで実装してください。

---

## 前提：計測して分かったこと（2026-08-10 取得）

- 直近90日でサイト全体のクリック **3件** / 表示 **10件**。データがあるのは08-05以降の4日間だけ
- 表示された tools のページは `/tools/sleep-cycle/` の1件のみ（表示1回・掲載順位42位）
- `/tools/sitemap.xml` は 33URL 送信・**エラー0**
- `npm test` は **216件すべて成功**（CLAUDE.md の記載は201件で、実態のほうが増えています）

URL検査（tools 13ページ）:

| 状態 | 件数 | ページ |
|---|---|---|
| 登録済み | 2 | `furusato-nozei`、`sleep-cycle` |
| 重複・旧ドメインが正規と判定 | 5 | `roulette`、`kosodate-shienkin`、`nenshu-kabe`、`shobyo-teate`、`aircon-denkidai` |
| 検出 - インデックス未登録 | 6 | `group`、`dice`、`warikan`、`hebon-romaji`、`hankaku-zenkaku`、`aspect-ratio` |

**13ページ中、検索結果に出られる状態なのは2ページだけです。**

### 301は正常に動いていることを実測で確認した

```
$ curl -sI https://tool.hasokon.com/nenshu-kabe/
HTTP/2 301
location: https://hasokon.com/tools/nenshu-kabe/
```

中間ホップのない単発の301で、転送先も正しいです。配信ページ側も
canonical・description・JSON-LD がビルド後の実HTMLに正しく出ており、gzip圧縮も効いています。

旧ドメインが正規と判定されている5件は**設定の不備ではなく再クロール待ち**です
（5件すべて最終クロールが統合デプロイの前日）。**tools 側の対応は不要**です。

ただし `hebon-romaji` / `hankaku-zenkaku` / `aspect-ratio` は
**旧サイトマップ経由でしか発見されていない**ので、旧サイトマップは移行完了まで残してください。

---

## 提案1：年収の壁の判定を「学生かどうか」から「年齢」に直す（最優先）

**SEOではなく、計算結果そのものが誤っています。**

### 根拠

`lib/nenshu-kabe.ts` の `Position` は立場と年齢と学生属性を1つの選択肢に畳んでいます。

```ts
type Position =
  | 'spouse'     // 配偶者の扶養内で働く（パート等）
  | 'student'    // 親の扶養内・19〜22歳の学生
  | 'dependent'  // 親の扶養内・その他
  | 'none';      // 扶養に入っていない
```

ところが、150万・159万・197万の壁の根拠になっている制度は
いずれも**年齢だけで決まり、学生であることを要件にしていません**。

| 制度 | 実際の要件 | 出典 |
|---|---|---|
| 健康保険の被扶養者認定（150万円未満） | 19歳以上23歳未満（配偶者を除く）。**学生である必要はない** | [日本年金機構](https://www.nenkin.go.jp/oshirase/taisetu/2025/202508/0819.html) |
| 特定親族特別控除（159万・197万） | 19歳以上23歳未満で生計を一にする親族。**学生要件なし** | [国税庁 No.1177](https://www.nta.go.jp/taxes/shiraberu/taxanswer/shotoku/1177.htm) |
| 勤労学生控除（163万） | 学生であること。**年齢要件はない** | 所得税法 |
| 短時間労働者の社会保険適用拡大（106万） | 週20時間以上・51人以上等。**昼間学生は適用除外** | [適用要件の解説](https://www.zeem.jp/useful/column/socialinsurance_parttimeworkers_3/) |

**年齢で決まるものを学生条件にし、学生で決まるものに年齢条件を付けている**取り違えです。

### 実際の出力（`evaluateKabe` を直接呼んで確認）

```
19-22歳の学生・51人以上・週20h   => 106万円の壁 / 119万 / 150万(学生) / 159万(学生) / 163万(学生) / 178万 / 197万(学生)
20歳フリーター(親の扶養/学生でない) => 119万 / 130万 / 136万(扶養) / 178万
25歳の大学院生(親の扶養)          => 119万 / 130万 / 136万(扶養) / 178万
```

3パターンすべてに誤りがあります。

1. **19〜22歳の学生に106万円の壁が出る** → 昼間学生は適用拡大の対象外なので、出るべきではない
2. **20歳のフリーター（学生でない）に130万円の壁が出る** → 正しくは**150万円**。
   さらに159万・197万（特定親族特別控除）が出るべきなのに出ない。
   **この層は扶養の範囲を20万円少なく案内されている**ことになります
3. **25歳の大学院生に163万円の壁（勤労学生控除）が出ない** → 勤労学生控除に年齢要件はないので、出るべき

2 の影響が一番大きいです。19〜22歳で学生でない人（既卒・フリーター・専門学校を出た人など）が
「親の扶養内・その他」を選ぶと、**実際には働ける20万円分を働かないよう案内してしまいます**。

### 提案する対応

`Position` を「立場」「年齢」「学生か」の3つの独立した入力に分解するのが素直です。

```ts
interface KabeInput {
  income: number;
  position: 'spouse' | 'dependent' | 'none';
  age19to22: boolean;   // 19歳以上23歳未満（12/31時点で判定）
  isStudent: boolean;   // 昼間学生
  size51: boolean;
  hours20: boolean;
}
```

各 `applies` の直し方:

| 壁 | 現在 | 直した後 |
|---|---|---|
| 106万 | `position !== 'none' && size51 && hours20` | `+ !isStudent` |
| 130万 | `spouse \|\| dependent` | `+ !(dependent && age19to22)` |
| 150万 | `student` | `dependent && age19to22` |
| 159万・197万 | `student` | `dependent && age19to22` |
| 163万 | `student` | `isStudent`（年齢を問わない） |

- `Calculator.tsx` に「19〜22歳」「学生」のチェックボックスを2つ足す
- ラベルの「（学生）」表記を「（19〜22歳）」に変える
- ページ本文とFAQの「学生」という説明を年齢ベースに書き換える
- **テストを先に足す。** 上の3パターンはそのまま回帰テストになります

### 期待できる効果

順位や流入への直接効果はありませんが、**このサイトの競合優位そのものを守る施策**です。

[docs/RESEARCH-2026-08.md](./RESEARCH-2026-08.md) は
「量産型サイトは改正が他制度へ波及する効果まで追えない、精度で勝てる」を戦略の柱に置いています。
その柱になっているページ自身が誤った金額を出している状態は、戦略と噛み合っていません。

年収の壁は YMYL 領域で検索エンジンも正確性を強く見ます。
**流入を増やす前に直すべき順序**だと考えます。

### 工数

**1〜1.5日**（ロジック半日 + テスト・UI・本文の修正）

---

## 提案2：2026年10月の「週20時間の壁」に先回りする（期限あり）

### 根拠

**2026年10月、106万円の壁（賃金要件・月8.8万円）が撤廃されます。あと約2ヶ月です。**
週の所定労働時間が20時間以上なら年収に関係なく加入対象になります（学生を除く）。
新たに約200万人が対象になる見込みです。

現在の `lib/nenshu-kabe.ts` はこの改正を **note の1文でしか扱っていません**。
CLAUDE.md にも「10月以降に note とページFAQの文言を見直すこと」と宿題として書かれています。

改正後は**106万円という金額の壁が消えます**。金額で判定している現在の作りのままだと、
10月以降は存在しない壁を表示し続けることになります。

### 提案する対応

**A. 既存ページを10月の改正に合わせて作り替える（必須・期限あり）**

- 106万円の壁を「週20時間の壁」として作り直す（金額ではなく労働時間の判定に変える）
- 施行日を定数に置き、施行前は現在の表示、施行後は新しい表示に切り替える
- ページ本文とFAQを書き換える
- 提案1と同じファイルを触るので、**まとめてやると手戻りが少ない**

**B. 社会保険の「働き損」計算機を新設する**

[docs/RESEARCH-2026-08.md](./RESEARCH-2026-08.md) が「最有力」と結論づけた案です。

> 「扶養内で働き続ける場合」と「加入する場合」を並べて比較し、手取りの逆転と損益分岐点を出すものがない

改正の2ヶ月前は、この調査結果を実行に移す好機です。
`lib/nenshu-kabe.ts` と `lib/furusato-nozei.ts` の令和8年分の税額計算をそのまま再利用できます。

ただし前回調査から2ヶ月経っているので、**着手前に競合の再確認をしてください**。
量産型サイトは改正当日に対応してくることが分かっています。

### 期待できる効果

- A は**やらないと10月に誤情報になる**ため、効果というより必須の保守
- B は RESEARCH の分析どおりなら空白のあるキーワード

### 工数

- A：1日（施行日での切り替えとテストを含む）
- B：3〜4日

**A は10月までに必ず終わらせる必要があります。**

---

## 提案3：内部リンクとパンくずを足す（SEO）

### 根拠

URL検査の「参照元」を見ると、発見経路が偏っています。

| ページ | 発見された参照元 |
|---|---|
| `group`、`dice` | `nenshu-kabe`（旧ドメインが正規と判定されている弱いページ） |
| `hebon-romaji`、`hankaku-zenkaku`、`aspect-ratio` | 旧サイトマップのみ |
| `warikan` | `sleep-cycle` |

新規サイトでは、サイトマップだけでクロールを引き出すのは難しいのが実情です。

### 提案する対応

1. **`/tools/` から `/games/` への導線を張る**（現在フッターは hasokon.com トップのみで2ホップ）
2. **BreadcrumbList の JSON-LD**（既存の `WebApplication` + `FAQPage` と同じ場所に追記）
3. `app/RelatedTools.tsx` の露出とリンク先の偏りを点検

### 期待できる効果

未クロールの6ページがクロール対象に入ります。
順位を上げる施策ではなく「土俵に乗せる」施策です。

### 工数

1〜1.5日

---

## その他、気づいたこと（提案には上げないが記録しておく）

- **OGP画像がない。** `app/layout.tsx` の `openGraph` は `type` / `siteName` / `locale` のみで
  `og:image` が未設定（home / games も同様）。共通1枚を作れば済みます（半日程度）
- **プライバシーポリシーが3箇所に重複。**
  `/privacy.html`（home）・`/tools/privacy/`・`/games/privacy/`。お問い合わせも2箇所。
  実際 home の `/privacy.html` が16位で表示されており、少ない表示回数がポリシーページに使われています。
  **AdSense の審査要件なので削除ではなく集約**にすること
- **CLAUDE.md のテスト件数が実態とずれている**（記載201件、実際216件）
- **フォームのラベルは問題ない。** `<label>` が入力要素を囲む形になっており、
  `htmlFor` がなくても関連付けは成立しています

---

## tools 側の優先順位

| # | 作業 | 効果 | 工数 |
|---|---|---|---|
| 1 | 年収の壁の判定を年齢ベースに直す | **大**（誤った金額を出している） | 1〜1.5日 |
| 2A | 10月の「週20時間の壁」対応 | **必須**（放置すると誤情報になる） | 1日 |
| 3 | 内部リンク・パンくず | 中〜大 | 1〜1.5日 |
| 2B | 社会保険の働き損計算機（新規） | 中〜大（要・競合再確認） | 3〜4日 |

**1 と 2A は同じファイル（`lib/nenshu-kabe.ts` と `app/nenshu-kabe/`）を触るので、
まとめてやると手戻りが少ないです。** どちらも流入を増やす施策ではありませんが、
流入が増えたときに困らないための施策です。
