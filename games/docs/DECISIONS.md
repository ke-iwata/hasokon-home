# 実装の記録（hasokon-games）

何を・なぜ作ったかの記録です。新しく機能を足したら、日付の新しい順に追記してください。
技術構成と運用の方針は [hasokon-tools](https://github.com/ke-iwata/hasokon-tools) に揃えています。

初回分（2026-08-10 時点）は git 履歴から復元したものです。
コミットメッセージから読み取れる範囲で書いているので、当時の意図と食い違う箇所があれば直してください。

---

## 2026-08-08：hasokon.com/games/ へドメイン統合した

`game.hasokon.com` から `hasokon.com/games/` へ移し、旧ドメインは301。

**理由**：サブドメインを分けていると、ドメインの評価が tools / games / home の3つに分散する。
1ドメインに集約したほうが、どのページの成果もサイト全体の評価に積み上がる。

## 2026-08-08：テスト環境ではサイト間リンクをテスト側ドメインに書き換えるようにした

**理由**：本番リンクが混ざると、テスト環境の確認中に本番へ飛んでしまい確認にならない。

## 2026-08-06：ファビコンを追加した（tools / home と同じ「h」アイコン）

**理由**：3サイトが1ドメインに乗るので、見た目の同一性を持たせる。

## 2026-08-06：フッターを hasokon.com へのリンクに変更した

**理由**：3サイトを回遊できるようにするため。

## 2026-08-06：4ゲームのUX改善（操作性・視認性・スマホ最適化）

**理由**：ゲームは滞在時間がそのまま価値になる。
特にスマホでの操作性は、遊べるか遊べないかを分ける。

## 2026-08-06：GA4の計測を開始した（tool.hasokon.com と同じ測定ID）

**理由**：まず計測を始めることを優先し、tools と同じストリームに相乗りした。
CLAUDE.md には「tools とは別のデータストリームを作る」と書いてあるので、
**現状は方針と実装がずれている**。サイト別に集計したくなったら分ける
（[2026-08-10の提案](./proposals/2026-08-10-seo-uiux.md) の3を参照）。

## 2026-08-05：ソリティア・スパイダーソリティア・マインスイーパー・2048 を追加した

**理由**：ブラウザですぐ遊べる定番ゲームは検索需要が大きく、
ルール自体が広く知られているので説明コストが低い。

## 2026-08-05：リバーシを削除した

**理由**：「オセロ」は登録商標。リバーシと呼べば名称の問題は避けられるが、
このときはLANアクセスの不具合対応と合わせて削除する判断をしている。
**名称の商標確認はゲームを追加するたびに必要**（CLAUDE.md 参照）。
「数独」もニコリの登録商標なので**ナンプレ**と呼んでいる。

## 2026-08-05：Basic認証つきテスト環境とタグリリースのフローを導入した

main へのpushで test.hasokon.com/games/、`v*` タグのpushで本番。

**理由**：tools / home と運用方式を揃えるため。3リポジトリで同じ手順にしておくと
運用の記憶が1つで済む。テスト環境に Basic認証をかけているのは、
テスト環境が検索にインデックスされて本番と重複扱いされるのを防ぐため。

## 2026-08-05：無料ミニゲーム集の初期実装（ナンプレ・リバーシ・ブロック崩し）

ゲームロジックは `lib/{slug}.ts` の純関数に置き、`Game.tsx` は入力と描画だけにしている
（ブロック崩しの物理演算も含む）。

**理由**：ロジックをUIから切り離すとテストできる。
ゲームは「進行が詰まる」「クリア判定が出ない」といった不具合が致命的なので、
境界値と終了条件をテストで押さえられる形にした。

---

## 提案中（未実施）

- [2026-08-10 SEO / UI・UX 改善提案](./proposals/2026-08-10-seo-uiux.md)
  — games は9ページすべてが未インデックス。相互リンク・パンくず・関連リンクの共通化
