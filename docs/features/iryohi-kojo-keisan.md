# 新ツール「医療費控除・セルフメディケーション税制 計算機」（いくら戻るか・どちらが得か）

**状態**：実装済み（2026-08-31・`stage: 'wip'`）。テスト環境で確認したら `preview` へ、
確定申告シーズン前（遅くとも2026年12月）に `public` へ上げる。
`public` にするPRで、`home/index.html`・`home/404.html` のカードと `home/llms.txt` の行、
`/tools/nenmatsu-chosei/` のFAQからのリンク差し替えも一緒に行う（`wip` の間は
noindex のページを公開ページからリンクしないため、まだ入れていない）。
**未実装**：年金収入の簡易対応（給与収入のみ受け付けている）。
**対象**：`tools/`（`hasokon.com/tools/iryohi-kojo/` を想定）
**起票**：2026-08-31

---

## 背景と根拠

### 「医療費控除 計算」は税金系で最大級の定番クエリで、サイトの得意分野そのもの

- 医療費控除は**年末調整では手続きできず、給与所得者でも確定申告が必要**な数少ない控除。
  「自分で計算して申告するか決める」需要が構造的にあり、
  「医療費控除 計算」「医療費控除 いくら戻る」「医療費控除 10万円以下」は
  毎年12月〜3月に大きく動く定番クエリ
- 本サイトの税・社会保険クラスタ（年末調整・ふるさと納税・高額療養費・年収の壁など）と
  完全に地続きで、**給与収入 → 課税所得 → 累進税率**の変換ロジックは
  `tools/lib/nenmatsu-chosei.ts` / `furusato-nozei.ts` に実装済み。流用できる
- [kogaku-ryoyohi.md](./kogaku-ryoyohi.md)（高額療養費）と対になる。
  「高額療養費（月単位・事前に負担を抑える）」と「医療費控除（年単位・後から税金が戻る）」の
  混同は定番の疑問で、相互リンク・FAQで両ページの回遊と検索面が広がる

### 2026年ならではの時事フック：セルフメディケーション税制が今年12月末で期限を迎える

セルフメディケーション税制（医療費控除の特例）は、5年延長された現行の適用期限が
**2026年（令和8年）12月31日**。延長の有無は令和9年度税制改正の議論待ちで、
秋〜年末に「今年で終わり？」「駆け込みで買うべき？」という検索・記事が増えるタイミング。
「対象のOTC医薬品をあといくら買えば控除ラインを越えるか」を出せる計算機は
この時期に固有の価値がある。

### 計測値（2026-08-31 取得）

- GSC（URLプレフィックス `https://hasokon.com/`・2026-08-01〜08-29・Search Analytics API）：
  「医療」「控除」「療養」「セルフメディケーション」を含むクエリの表示は**0件**。
  `/tools/kogaku-ryoyohi/`・`/tools/nenmatsu-chosei/`・`/tools/shobyo-teate/` の
  ページ別データも無し（サイト全体で 5クリック・118表示・平均39.1位）
- GA4（hasokon-property・直近28日・Data API）：`/tools/kogaku-ryoyohi/` は 2PV。
  `/tools/nenmatsu-chosei/` は表示上位60パスの圏外

つまり**税クラスタの既存ページにまだ検索実績はなく、「医療費控除 計算 が
税金系で最大級の定番クエリ」という需要規模の主張は定量データなし・定性判断**
（一般的なSEO知識と、12〜3月に解説記事が毎年量産される事実に基づく）。
受け皿ページが無い以上サイト内データでは需要を測れないので、公開後にGSCで
実測して答え合わせする前提の提案である。

### 競合状況

保険会社・税理士サイトに解説記事は多いが、**その場で両制度を並べて比較計算できる
無料ツールは少ない**（多くは解説＋数式のみ、または医療費控除単体の簡易計算）。
「両方計算してどちらが得か」を1画面で出すのが差別化点。

## 制度の要点（実装時に一次情報で再確認すること）

| 項目 | 医療費控除（所得税法73条） | セルフメディケーション税制（租税特別措置法41条の17の2） |
|---|---|---|
| 控除額 | （支払医療費 − 保険金等の補填額）−（**10万円** と **総所得金額等×5%** の小さい方） | 対象OTC医薬品の購入額 − **1.2万円** |
| 上限 | 200万円 | 8.8万円 |
| 要件 | 生計を一にする家族分を合算可 | 健康診断・予防接種等の「一定の取組」が必要 |
| 適用期限 | 恒久 | **2026年12月31日**（延長は令和9年度税制改正待ち） |
| 併用 | **選択適用（併用不可）** | 同左 |

- 戻る額の概算は**所得税と住民税の2段構え**で示す。所得税は申告後1〜2か月で還付、
  **住民税は翌年度（6月からの1年分）の税額が控除額×10%ぶん安くなる**。この時期の違いを
  明示するのが分かりやすさの肝（「10万円戻る」ではなく「控除が10万円で、戻るのはその2〜5割。
  しかも一部は来年の住民税」）
- **所得税の減税額は「控除額×限界税率」で出さない。** 控除額が税率ブラケットを
  またぐと過大表示になる（例：課税所得335万円・税率20%の人が控除40万円を受けると、
  下の10%帯に落ちる20万円ぶんは10%でしか減らない）。
  `tools/lib/nenmatsu-chosei.ts` の `calcYearTax()` を**控除を入れる前と後で2回呼び、
  年税額の差を還付額とする**（復興特別所得税1.021込み）。限界税率は「あなたの税率は20%」
  という内訳の説明表示にだけ使う
- 「総所得金額等×5%」の分岐により、**総所得200万円未満（給与収入だと約297万円未満）なら
  医療費が10万円以下でも控除できる**。これが最頻の誤解（「10万円超えないと無理」）で、
  FAQ・結果表示で必ず拾う

## 現状

医療費控除を扱うページはまだ無い。既存ページとの重複は
`/tools/nenmatsu-chosei/` のFAQが「医療費控除は年末調整では戻らない（確定申告が必要）」と
1問だけ答えている程度で、計算はどこにも無い。カニバリを避けるため、
医療費控除の説明・計算は本ツールに一本化し、nenmatsu-chosei 側のFAQは
新ページへのリンクに差し替える。

## 提案する仕様

1. **入力**：①年収（給与収入。パート・年金は簡易対応）②その年に支払った医療費の合計
   ③保険金などで補填される金額 ④対象OTC医薬品の年間購入額（任意）
   ⑤健康診断等の取組の有無（チェックボックス。セルフメディケーション税制の要件）
2. **出力**：
   - 医療費控除の控除額と、戻る額の概算（所得税の還付＋住民税の軽減を分けて表示）
   - セルフメディケーション税制の控除額と戻る額の概算（要件を満たす場合のみ）
   - **どちらが得かの判定**と、その差額
   - 足切り未満の場合は「あといくらで控除が始まるか」を表示（駆け込み判断に効く）
   - **スマホでは2制度の比較を横並びの表にしない**（幅390pxで最も崩れやすい形）。
     **縦積みのカード2枚＋差額の1行**を既定のレイアウトにする
3. **給与収入 → 課税所得・年税額**は `lib/nenmatsu-chosei.ts` の `calcYearTax()` を流用し、
   **控除前後の2回計算の差分**で所得税の還付額を出す（「制度の要点」の注意どおり。
   単一の限界税率を掛けない）。社会保険料は既存ツールと同じ簡易推計でよい。
   **分母の取り違えに注意**：足切りの5%の分母は**総所得金額等**（給与所得控除後・
   所得控除前）、税率の適用対象は**課税所得**（所得控除後）で、**別の値**。
   ここを混同すると低所得帯で「10万円以下でも控除できる」という本ツール一番の売りが
   逆に間違った答えを出す。
   免責は `tools/app/nenmatsu-chosei/Calculator.tsx` の「**この結果は概算です。**」と
   同じ形に揃える
4. **静的コンテンツ**（SEOの受け皿）：
   - 対象になる費用・ならない費用の早見表（通院交通費○・自家用車のガソリン代×など定番）
   - 「高額療養費との違い」（`/tools/kogaku-ryoyohi/` と相互リンク）
   - 「年末調整ではできない・確定申告のやり方」（`/tools/nenmatsu-chosei/` と相互リンク）
   - **申告し忘れは過去5年分さかのぼって還付申告できる**こと（「医療費控除 過去分」
     「さかのぼり」は毎年動く定番クエリ。計算機の入力は変えず文章だけで拾う）
   - **戻る時期**：所得税は申告後1〜2か月で還付、住民税は翌年度6月からの税額に反映
     （「いつ振り込まれるか」の定番疑問をFAQで拾う）
   - **医療費控除を使うとふるさと納税の控除上限が下がる**こと
     （`lib/furusato-nozei.ts` の `FurusatoInput` に「その他の所得控除」の入力が
     既にある実在の相互作用。`/tools/furusato-nozei/` と相互リンクし、税クラスタの回遊に効かせる）
   - セルフメディケーション税制の期限と延長議論の現在地（**状態を日付つきで書く**。
     税制改正大綱が出たら更新する運用を状態行に残す。**「今年で終わり」と断定せず
     「現時点の期限は2026年12月31日」と書く**——延長された瞬間に嘘になる文言を置かない）
5. **FAQ + JSON-LD**（FAQPage）・`robots: robotsFor('iryohi-kojo')`・
   stage は `wip` から。既存ツールの作法（mobile-one-screen・game-recordsは対象外）に従う
6. **テスト**：控除額の境界（10万円 / 総所得×5% の分岐、200万円・8.8万円の上限、
   補填額が医療費を超えるケース、マイナスにならないこと）を `tools/tests/` に置く。
   **足切り分母の境界テストを必ず含める**：給与収入297万円前後（総所得金額等200万円の境界）で
   足切り額が 10万円 ↔ 総所得×5% に正しく切り替わること（分母を課税所得と
   取り違えると落ちるテストにする）

### 触るファイル

| ファイル | 変更 |
|---|---|
| `tools/lib/iryohi-kojo.ts`（新規） | 計算ロジック |
| `tools/app/iryohi-kojo/page.tsx` ほか（新規） | ページ・UI |
| `tools/lib/registry.ts` | エントリ追加（`stage: 'wip'` から） |
| `tools/tests/` | 境界テスト |
| `home/index.html` / `home/404.html` | サイト一覧に1行追加（**`public` にするPRで**。`home/` にはビルド工程が無く `stage` が効かないため手で足す） |
| `home/llms.txt` | 1行追加（**`public` にするPRで**。`scripts/test/llms-txt.test.mjs` が registry との食い違いを検知する） |

## 期待される効果

| 効果 | 測り方 |
|---|---|
| 12〜3月の「医療費控除 計算」系クエリの受け皿（税クラスタで最も検索量の大きい未対応キーワード） | GSC で `/tools/iryohi-kojo/` の表示・クリック |
| セルフメディケーション税制の期限（2026年末）という時事の受け皿 | 「セルフメディケーション 2026 終了」系クエリの表示 |
| 高額療養費・年末調整ページとの回遊 | GA4 のページ遷移 |

## 工数の見積り

| 作業 | 消費トークン（目安） |
|---|---|
| 制度の一次情報確認（国税庁タックスアンサー・e-Gov条文） | 40k |
| `lib/iryohi-kojo.ts`（計算ロジック・境界テスト込み） | 80k |
| ページ（入力UI・結果表示・静的コンテンツ・FAQ・JSON-LD） | 120k |
| テスト・調整・`preview` 確認 | 60k |
| **合計** | **約300k** |

## やらないこと

- **確定申告書の作成・出力**（国税庁の確定申告書等作成コーナーの領分。リンクで案内する）
- **医療費の明細管理**（件数入力・CSV取り込みなどはしない。合計額を入れる設計に絞る）
- **セルフメディケーション対象品目の検索**（品目リストは厚労省の一覧へリンク）
- 住民税の細かい調整控除・所得割非課税の判定（「概算」の注記で扱う）

## 一次情報（実装時に必ず確認）

- 国税庁 タックスアンサー No.1120「医療費を支払ったとき（医療費控除）」
  https://www.nta.go.jp/taxes/shiraberu/taxanswer/shotoku/1120.htm
- 国税庁 タックスアンサー No.1129「セルフメディケーション税制」
  https://www.nta.go.jp/taxes/shiraberu/taxanswer/shotoku/1129.htm
- 厚生労働省 セルフメディケーション税制（対象品目・適用期限）
  https://www.mhlw.go.jp/stf/seisakunitsuite/bunya/0000124853.html
- e-Gov 所得税法73条・租税特別措置法41条の17の2（条番号は実装時に照合する）
