# 全ゲームに記録（ベスト・勝敗・クリアタイム）を揃える

**状態**：実装（2026-08-13）。**2026-09-19 に連続日数の2項目を追加**
（[game-hoshioki-puzzle.md](./game-hoshioki-puzzle.md) の日替わり基盤。下記「連続日数」）
**対象**：`games/`（全10ゲーム）
**起票**：2026-08-13

---

## 背景と根拠

### 記録が残るゲームと残らないゲームが混在している

`grep -rln "localStorage" games/app games/lib`（2026-08-13 実施）の結果、
プレイ記録を保存しているのは **10本中4本だけ**。

| 記録あり | 記録なし |
|---|---|
| 2048（ベストスコア）・リバーシ・ノノグラム・フリーセル | **ソリティア・スパイダー・マインスイーパー**・ナンプレ・麻雀ソリティア・ブロック崩し |

問題は、**「記録なし」側にアクセス上位のゲームが集中している**こと。
GA4（2026-08-13 取得 / GA4 Data API `runReport`、`properties/548154955`）の
直近28日のページビューは、ソリティア系35・マインスイーパー39・スパイダー35で、
ゲーム中トップ3がすべて記録なし側にある（新旧パス合算。
[ga4-page-path.md](./ga4-page-path.md) の重複期間のため合算で数えた）。

### なぜ記録が滞在時間・再訪に効くか

games/docs/DECISIONS.md は「ゲームは滞在時間がそのまま価値になる」と定義している。
ソリティア・マインスイーパーのような定番ゲームは他サイトでいくらでも遊べるので、
**「自分の記録がここにある」ことが、このサイトで遊び続ける数少ない理由になる**。
勝率・ベストタイムは「もう1回」の動機そのもので、
定番ゲームサイト（Solitr・Minesweeper Online 等）はほぼ例外なく統計を持っている。

## 現状

- ソリティア・スパイダー・マインスイーパー・ナンプレ・麻雀ソリティア・ブロック崩しは、
  クリアしても何も残らない。リロードすると痕跡が消える
- 記録を持つ4本も実装がバラバラ（キー名・保存形式が各ゲームの `Game.tsx` 内に直書き）

## 提案する仕様

### 共通モジュール

`games/lib/records.ts` を新設する（純関数＋薄いストレージ層）。

- キーは `hasokon-games:records:<gameId>:v1` に統一。値はJSON
  （`plays`・`wins`・`bestTimeMs`・`bestScore`・`bestMoves` のうちゲームが使うものだけ）
- SSR・`localStorage` 不可（プライベートモード等）でも落ちないよう
  try/catch で包み、読めなければ「記録なし」として動く
- 既存4本の旧キーは初回読み込み時に新形式へ移行する（読めたら移して旧キーを消す）

### 連続日数（2026-09-19 追加）

日替わり（今日の1問）を持つゲームのために、`RecordEntry` に2項目を足した。
**全ゲーム共有の型なので、黙って足さずにここに書く**
（決定の経緯は [game-hoshioki-puzzle.md](./game-hoshioki-puzzle.md) の「連続日数を入れる器」）。

| 項目 | 内容 |
|---|---|
| `lastClearedOn?: string` | 日替わりを最後にクリアした**端末のローカル日付**（`'2026-09-18'`） |
| `streak?: number` | その日の時点での連続日数 |

- **数え方は `games/lib/daily.ts` に置く**（`nextStreak` / `currentStreak`）。
  記録側は器だけを持ち、日付の作り方（`localDateKey`）も同じモジュールに揃える。
  **シードと連続日数で日付の取り方が違うと、日付をまたぐ時間帯に記録が壊れる**
- `PlayResult` に `clearedOn?` を足した。**渡したときだけ**連続日数を数え直す
  （渡さない既存のゲームは、この2項目に一切触れない）
- **この項目を持たない記録（これ以前の保存）を読んでも落ちない。**
  `streak` が無ければ 0 とみなして数え直す（`tests/records.test.ts`）
- **`clearedIds` に日付を積む案は採らなかった**（配列が無限に伸びるため）
- まとめる（`mergeEntry`）ときは**日付と連続日数をセットで選ぶ**。
  別々に「大きいほう」を取ると、古い日付に新しい連続日数が付いた組み合わせができる
- **localStorage で持つ以上、端末・ブラウザをまたぐと消える。**
  静的サイト（サーバー・アカウントを持たない）なのでこれは受け入れ、画面にも明記する

### 各ゲームへの組み込み

| ゲーム | 残す記録 |
|---|---|
| ソリティア・スパイダー・フリーセル・麻雀ソリティア | 勝利数／プレイ数・ベストタイム・（あれば）最少手数 |
| マインスイーパー | 難易度別ベストタイム・勝率 |
| ナンプレ・ノノグラム | 難易度別ベストタイム・クリア数 |
| 2048・ブロック崩し | ベストスコア（現状の2048の記録は移行） |
| リバーシ | 対CPU戦績（現状の記録は移行） |

表示は2箇所：盤面上部の小さな帯（例「ベスト 2:41」）と、
クリア／ゲームオーバー画面（今回の結果とベストの比較。ベスト更新時は明示）。

### 記録が消えるケースと、その扱い（2026-08-13 コメント対応で追記）

`localStorage` に標準の有効期限は無く、Chrome・Edge・Firefox では
ユーザーが閲覧データを消すか、端末の空き容量逼迫による退避が起きない限り残り続ける。
ただし**消えるケースは実在する**ので、仕様として織り込む。

| 消えるケース | 内容 |
|---|---|
| **Safari（iPhone/iPad/Mac）のITP** | サイトを**7日間訪問しないと**スクリプトが書いたストレージ（localStorage・IndexedDB とも）が削除される。最大の現実リスク |
| ユーザーによる履歴・サイトデータの削除 | ブラウザ共通 |
| プライベートブラウズ | セッション終了で消える（保存自体は動く） |
| 別ブラウザ・別端末 | そもそも共有されない |

対策として次の2つを仕様に含める。

1. **保存の実態を最初から明示する**：記録表示の近くに小さく
   「記録はこの端末のブラウザに保存されます。ブラウザのデータ削除や、
   iPhone/Mac の Safari では7日間訪問がない場合に消えることがあります」の
   注記を置く（初回だけ目立たせ、以後は折りたたみでよい）。
   「急に消えた」と感じさせないためには、**消える前に知らせておく**のが唯一の手
2. **消えていても壊れない**：読めなければ「記録なし」の初期状態から
   静かに再スタートする（エラー表示や訴求はしない）

なおこの機能の主対象は**繰り返し遊びに来る人**で、訪問のたびに Safari の
7日カウントはリセットされる。削除に当たるのは7日以上戻らなかった人に限られ、
その場合も失われるのは端末内の数値だけで、実害よりも「黙って消える」ことの
体験の悪さが問題になる。だからこそ注記（対策1）を仕様の一部とする。

サーバー保存で根本解決する案は、アカウント・API を持たない静的配信の方針に
反するため採らない（「やらないこと」参照）。`navigator.storage.persist()` は
Safari の ITP による削除には効かないため、解決策にならない。

### テスト

`games/tests/records.test.ts` — 保存・読み出し・旧キー移行・壊れたJSONの無視・
ストレージ不可時のフォールバックを網羅する。
連続日数（2026-09-19 追加）はここに加えて、**この項目を持たない記録を読んでも落ちないこと**・
1日空くと1に戻ること・同じ日に2回クリアしても増えないことを固定する。
日付そのものの計算は `games/tests/daily.test.ts`。

## 期待される効果

- 再訪の動機づけ。GA4の**リピーター率**と**平均エンゲージメント時間**
  （とくに `/games/solitaire/`・`/games/minesweeper/`・`/games/spider/`）を
  導入前後4週間で比較する
- 実装の統一により、今後の新ゲーム（例：[game-block-puzzle.md](./game-block-puzzle.md)）は
  `records.ts` を呼ぶだけで記録がつく

## 工数の見積り

- 実装1セッション。消費トークンの目安：**6万〜10万**
  （共通モジュール＋テストで2〜3万、6ゲームへの組み込みで3〜5万、
  既存4ゲームの移行で1〜2万）
- ゲームロジックには触らない（クリア判定のコールバックに1行足すのが基本形）

## やらないこと

- **サーバー側への記録・ランキング** — アカウントも API も持たない方針
  （静的配信のみ）のため。端末内で完結させる
- **プレイ履歴の時系列グラフ** — まず集計値だけ。需要が見えたら検討
- **ブロック崩しへのタイム記録** — スコア型のゲームにタイムは馴染まない。
  ベストスコアのみ
