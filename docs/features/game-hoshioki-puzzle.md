# 星置きパズル（Star Battle 系）— 各行・各列・各ブロックに星を1つずつ置く純ロジックパズル。日替わり1問つき

**状態**：提案（2026-09-18 起票、未実装）。実装は `stage: 'wip'` から。
**`public` への昇格は [google-index-recovery.md](./google-index-recovery.md) の解除判断後**（#218・#220・#223・#224 と同じ運用）。
`public` にする PR では `home/index.html` のカード・`home/404.html` の一覧・`home/llms.txt` の行も一緒に足す。
**対象**：`games/`（`hasokon.com/games/hoshioki-puzzle/` を想定）
**起票**：2026-09-18

---

## 背景と根拠

### いま伸びているのは「数独の変種」で、当サイトの強い客層と重なる

- 2026-09-18 時点の App Store 日本・無料ゲームランキング 1位は、同じルール（各行・列・領域に印を1つ、
  斜めも隣接不可）を猫で味付けした『Meowdoku!』（Oakever Games、2026年6月配信・日替わり問題つき）。
  海外では LinkedIn Games の『Queens』が同ルールで、LinkedIn Games は 2026年5月時点で 1日 350万ログイン
  （年初は約 200万）。Netflix も『Starstruck』として同ルールを日替わり配信している
  （2026-09-18 Web 調査。App Store のチャートは日々動くので、実装時に再確認する）
- **当サイトで最も長く遊ばれているのはロジック系**。GA4（プロパティ 548154955、2026-09-04〜09-17、2026-09-18 取得）の
  `tool_use` イベントは `/games/block-puzzle/` 358・`/games/minesweeper/` 145・`/games/nonogram/` 26・`/games/nanpre/` 3。
  訪問者は Japan 223 セッション中 desktop 195（85%）で、[game-hakoiri-musume.md](./game-hakoiri-musume.md) が示した
  「45歳以上・PC」の層。ナンプレ・ノノグラムと同じ「盤面をクリックで埋める」操作がそのまま使える
- **日替わり 1問を持つゲームがサイトに無い。** ナンプレ・ノノグラムはランダム生成のみ。
  日付をシードにした決定論的生成なら、サーバー無しで「今日の1問」と連続記録が作れる。
  再訪の動機はいまのサイトの弱点（GA4 の `first_visit` 162 / `session_start` 228 で、7割が初回）

### 権利関係は無害

- ルールは 2003年の世界パズル選手権で公開された公知のもので、ルール自体は著作権の対象にならない
  （個々の問題を写さなければよい。問題は自前で生成する）
- **使わない名前**：「Queens」（LinkedIn）、「Meowdoku」（Oakever）、「Two Not Touch」（NYT／krazydad）、
  「スターバトル」の英語圏での商標は追い切れないので、名前・slug・見出しには使わない。
  猫のモチーフも Meowdoku の模倣に見えるので使わない
- 名前は**「星置きパズル」**、slug は `hoshioki-puzzle`。日本の登録商標は 2026-09-18 の検索では見つからなかったが、
  **`public` にする前に J-PlatPat で「星置き」「ほしおき」を確認する**（[game-nikakudori.md](./game-nikakudori.md) と同じ運用）

## 提案する仕様

### ルール

- N×N の盤面が N 個の連結した領域（太枠）に分かれている
- 各行・各列・各領域に星を**ちょうど 1つ**置く（10×10 は 2つ置く「2つ星」も持てるが、初版は 1つ星のみ）
- 星どうしは**斜めを含めて隣接しない**
- 解は 1通り（生成時にソルバーで一意性を保証する）

### サイズと難易度

| モード | 盤面 | 目安 |
|---|---|---|
| かんたん | 5×5 | 1〜2分。導入 |
| ふつう | 7×7 | 3〜5分 |
| むずかしい | 9×9 | 5〜10分 |
| **今日の1問** | 7×7（日付シード） | 全員が同じ問題。クリア時間と連続日数を記録 |

### 操作（games/CLAUDE.md の「画面の約束」どおり。スマホ・PC 両方）

- マスをタップ／クリック：空 → ×（候補消し）→ ★ → 空 の順に切り替え（ナンプレのメモと同じ発想）
- 右クリック／長押し：直接 ★
- 星を置いたとき、同じ行・列・領域と周囲 8マスに**自動で ×** を入れるオプション（既定 ON。解く楽しさを削らない範囲の補助）
- 矛盾（同じ行に 2つ、隣接）は赤で示す。**答えは教えない**（ヒント無し）
- 「もどす」「最初から」「チェック（矛盾の有無だけ）」

### 画面

- 盤面は正方形で、幅は `min(100vw − 32px, 60vh)`。9×9 でも 390px 幅で 1マス 38px 以上
- 領域の境界は太線、マスの背景は領域ごとに淡い色分け（色覚に配慮して**線でも区別できる**ようにする）
- クリア画面：かかった時間、今日の1問なら連続日数と「結果をコピー」（絵文字の盤面。`navigator.clipboard`。URL を添える）
- 記録は `lib/records.ts`（[game-records.md](./game-records.md)）に乗せる。今日の1問は `records:hoshioki-puzzle:daily` に日付・時間・連続日数

### 生成（`games/lib/hoshioki-puzzle.ts`。純関数）

1. 星の配置を先に決める（N クイーン＋隣接禁止の制約をランダム探索で 1つ出す）
2. 各星を種にして領域を成長させ、N 個の連結領域に分割する（成長順をランダムに）
3. バックトラック・ソルバーで解の個数を数え、**1通りでなければ 2 からやり直す**
4. 難易度は「ソルバーが使った推論の段数」で測り、モードごとの範囲に入るまで生成する
5. 今日の1問は `seed = YYYYMMDD` の PRNG（`mulberry32` など）で 1〜4 を決定論的に回す。
   **生成の手順を変えると過去の「今日の1問」が変わるので、`DAILY_GENERATOR_VERSION` を持ち、
   変えたら記録側のキーも変える**

### ページ構成

- `games/app/hoshioki-puzzle/page.tsx`（`metadata` に `robots: robotsFor('hoshioki-puzzle')`）・`Game.tsx`
- `games/lib/registry.ts` に `{ slug: 'hoshioki-puzzle', name: '星置きパズル', stage: 'wip' }`。
  `description` は「各行・各列・各ブロックに星を1つずつ。斜めも隣り合わない。5×5〜9×9の3段階と、毎日変わる今日の1問」
- `GameIcon.tsx` に星のアイコン
- 解説：ルール・解き方のコツ（「1マスしか候補が無い領域から埋める」「行に閉じ込められた領域」）・FAQ・JSON-LD

### テスト（`games/tests/hoshioki-puzzle.test.ts`）

- ソルバー：既知の 5×5 問題で解が 1通り。星を 1つ動かした盤面で 0通り
- 生成：1,000 回生成して全件が一意解・連結領域・領域数 = N
- 日付シード：同じ日付で同じ盤面。`DAILY_GENERATOR_VERSION` を変えると盤面が変わる
- 操作：タップの状態遷移・自動 × の範囲・矛盾検出
- `tests/stage.test.ts` が `robots` の書き忘れを落とす（既存）

## 期待される効果

- **新規の検索語**：「星置き パズル」「スターバトル 無料」「Queens パズル 日本語」は、
  日本語の無料ブラウザ版がほぼ無い（2026-09-18 検索。英語の krazydad・puzzle-star-battle.com が上位）。
  流行の中心（Meowdoku・Queens）はアプリで、「ブラウザで今すぐ」の枠が空いている
- **再訪**：今日の1問と連続日数で `session_start` に対する `first_visit` の比率（いま 71%）が下がるかを見る。
  測り方：GA4 `pagePath = /games/hoshioki-puzzle/` の `activeUsers` に対する `sessions` の比
- **既存資産の流用**：盤面 UI・記録・stage 運用はナンプレ／ノノグラムのもの。次の「たし算ナンプレ」にも同じ基盤が使える

## 工数の見積り

| 作業 | 消費トークン（目安） |
|---|---|
| `lib/hoshioki-puzzle.ts`（配置・領域分割・ソルバー・難易度・日付シード）＋テスト | 80k |
| `Game.tsx`（タップ状態遷移・自動 ×・矛盾表示・もどす・クリア画面・結果コピー） | 70k |
| `page.tsx`・`GameIcon.tsx`・registry・記録のキー・stage テスト・スマホ実測（390×844 / 320×568） | 30k |
| J-PlatPat の名称確認・DECISIONS.md | 10k |
| **合計** | **約 190k** |

## やらないこと

- **ヒント（次の 1手）。** 純ロジック系は「自力で見つける」が価値。矛盾の表示だけ
- **2つ星モード（10×10 に星 2つ）。** 生成と難易度調整が別物。初版で 1つ星の反応を見てから
- **サーバーでの日替わり配信・ランキング。** 静的サイトの方針に反する。日付シードで代替する
- **他サイトの問題の転記。** 生成器で作る（著作権と、サイトの「コンテンツ＝コード」の型の両方から）
- **猫・動物の絵柄。** Meowdoku の模倣に見える。星で通す
