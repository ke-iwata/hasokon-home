# 追記型のログ（新しいエントリを先頭に足していくファイル）は union マージで
# 自動統合する。機能実装を並走させると、複数ブランチが同じ位置（区切りの直下）に
# エントリを足すため毎回コンフリクトしていた対策（docs/README.md の約束も参照）。
#
# union はコンフリクトマーカーを出さず両方の行を残す。行単位の機械的な統合なので、
# まれにエントリの順序が入れ替わることがあるが、時系列ログなので手で直せば済む。
# ふつうの散文やコードには使わないこと（黙って混ざるため）。
docs/DECISIONS.md merge=union
tools/docs/DECISIONS.md merge=union
games/docs/DECISIONS.md merge=union
